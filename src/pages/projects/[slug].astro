---
import ProjectView from '../../views/ProjectView.astro';
import { getProjects, toPaths } from '../../lib/content';

export async function getStaticPaths() {
  return toPaths(await getProjects('zh-tw'));
}
const { entry } = Astro.props;
---
<ProjectView entry={entry} locale="zh-tw" />
